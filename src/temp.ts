// Temporary createPods function until the corresponding
// smart contracts are merged + deployed.
/* eslint-disable */
export async function createPods(
  controller,
  members,
  thresholds,
  admins,
  labels,
  ensStrings,
  imageUrls,
) {
  console.log('yerda');
}
